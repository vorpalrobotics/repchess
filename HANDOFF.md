# Handoff

A snapshot for the next session, not a standing doc. Delete it once it stops
being true.

- **Perfect Opening deliberately has no help topic.** The user considers it
  an experiment and isn't sure it will stay in the app, so don't document it
  in `help/` without asking. Everything else in the hamburger menu is covered.
